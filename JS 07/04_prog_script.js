let peso,altura,imc;

imc=indice()
alert(calculo)
function indice(){
    peso=prompt("Digite o peso do paciente: ")
    altura=prompt("Digite a altura do paciente: ")
    calculo = peso/(altura**2);
}